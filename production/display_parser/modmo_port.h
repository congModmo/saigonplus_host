#ifndef __MODMO_PORT_H_
#define __MODMO_PORT_H_

#include <stdint.h>
#include <stdbool.h>

//#define ASSERT_RET(cond, msg, ret) \
//    if (!(cond))                   \
//    {                              \
//        debug(msg);                \
//        return ret;                \
//    }

//#define TYPEDEF_PACKED_STRUCT typedef struct __packed
//#define PACKED_STRUCT struct __packed
//#define TYPEDEF_PACKED_UNION typedef union __packed
//#define PACKED_UNION union __packed

#endif 
